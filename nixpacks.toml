[phases.setup]
nixPkgs = { php = "php82" }

[phases.install]
cmds = [
  "composer install --no-dev --optimize-autoloader",
  "npm ci"
]

[phases.build]
cmds = [
  "npm run production"
]

[start]
cmd = "php artisan serve --host=0.0.0.0 --port=$PORT"

